import { AdminDesk } from "@/components/AdminDesk";

export const metadata = {
  title: "Admin — BLOCK DICE",
  description: "Monitor every create, join, and settle transaction.",
};

export default function AdminPage() {
  return <AdminDesk />;
}
